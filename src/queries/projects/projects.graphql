query GetProjects {
  projectCollection {
    skip
    limit
    total
    items {
      slug
      name
      url
      summary
      tags
      funFact
      description {
        json
      }
    }
  }
}
